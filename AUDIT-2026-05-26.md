# 소라고동 codebase audit — 2026-05-26

마커 위생 + 타입 가드 전수 검사. 6개 영역 병렬 audit 결과 통합.

## Executive Summary

**총 발견**: 55 항목
- CRITICAL: 12
- HIGH: 22
- MEDIUM: 16
- LOW: 5

**두 가지 패턴**:
- **A. 마커 위생** (Pattern A): 객체 다중 마커 중 한쪽만 바뀌어 일관성 깨짐 → 좀비 / 영구 stuck / 부분 적용
- **B. 타입 가드 누락** (Pattern B): 외부 데이터(LLM 응답, Supabase, localStorage)에 타입 가정 없이 메소드 호출 → throw / dirty data 영구 저장

**가장 위험한 4개** (즉시 fix 권장):
1. `06-extract-insight.js` trait/value/pattern name typeof 가드 누락 — forceAnalyze 무한 루프와 동일 패턴, 다른 경로에서도 발화 가능
2. `_aiSummaryFailed` sentinel이 사용자가 일기 직접 작성 후에도 strip 안 됨 — silent 일기 분석 freeze
3. weekly review `scenes` 스키마 mismatch (string vs object) — 주간 리뷰 핵심 카드 영구 hidden
4. session refresh 응답에서 `user` 누락 시 crash + signup `refresh_token` 누락 시 1h logout

---

## 발견 — 영역별 상세

### 영역 1: Archive 마커 (30-force-analyze.js, 19-chat/, 23-archive/, 25-archive-daily/)

#### CRITICAL
- **A1-C1** `_extractFromIndex` clear 지점 0개 — 이어서-마무리 두 번 반복한 archive의 옛 부분이 영영 분석에서 제외
  - 위치: set at `19-chat/06-chat-plus-menu.js:101`, consumer at `:23` & `30-force-analyze.js:459,1051,1081`
  - Fix: `_resumeChapterCleanupBatch:1289` 의 `_caseAnalysisDone && _topicExtractDone` 분기에 `delete arch._extractFromIndex`
- **A1-C2** `_cleanupFailedAt` 박힌 archive — 분석 결과 한 번도 적용 안 됐는데 UI에 표시 X + 수동 retry 불가
  - 위치: `30-force-analyze.js:1332-1335` set, read 지점 0개
  - Fix: `25-archive-daily/01-chat-archive-modal.js:103` 에 "❌ 분석 실패 — 재시도" 버튼 + 수동 retry path

#### HIGH
- **A1-H1** multi-device race: `state.pendingChapterCleanupBatch` 가 null인데 archive에 `_batchSubmittedAt` 잔존 → 12h 차단
  - Fix: sweep (1827~) 에 `!state.pendingChapterCleanupBatch && a._batchSubmittedAt` 케이스 strip
- **A1-H2** `_caseAnalysisDone` / `_topicExtractDone` 세션 단위 마커 → 부분성공 retry 시 영구 stuck 가능
  - Fix: ISO timestamp `_caseAnalysisAt` / `_topicExtractAt` 로 변경, 둘 다 있으면 `_cleanedAt` 박음
- **A1-H3** `_hasMessages=true` 박혔는데 chat_messages row가 cloud cascade로 사라진 archive — lazy hydrate 영원 fail
  - Fix: `_loadChapterMessages` 결과 empty 시 `_hasMessages` clear

#### MEDIUM
- **A1-M1** `_cleanupFailedAt` 좀비 (위 C2와 연결)
- **A1-M2** `_chatMessagesSaveInFlight` 가 tab close 시 영구 잔존 → backfill 영원 skip
  - Fix: init boot-time sweep `delete a._chatMessagesSaveInFlight` 한 줄
- **A1-M3** `_deleted` only / `_deletedAt` 없는 옛 archive — 7일 hard delete bypass (의도된 design)
- **A1-M4** `_extractFromIndex` 복원 후 stale (invariant 약함)

#### LOW
- **A1-L1** `_pushedToMagicReflection` 마커 부재 — 중복 magic/reflection archive 가능 (호출자 순서 의존)

---

### 영역 2: Entry/Diary 마커 (30-force-analyze.js, 17-mood-vitality-sleep.js, 14-timeline-lens.js)

#### CRITICAL
- **A2-C1** 일기 직접 작성 + batch 결과 동시 도착 → day-modal에 일기 + 🤖 자동요약 둘 다 표시
  - 위치: `30-force-analyze.js:880-888, 1262-1271`
  - Fix: entry update 분기에 `if (entry.diary) continue;` 가드 추가 (build 시점 + result 시점 둘 다)
- **A2-C2** `_aiSummaryFailed` sentinel — 사용자가 그 옛 날에 일기 추가하거나 챗 보충해도 batch 재시도 영원 X
  - 위치: set at `30-force-analyze.js:886-887, 1267-1268`, clear 지점 0개
  - Fix: `17-mood-vitality-sleep.js:178-180`, `19-chat/05-send-chat.js:104-106` (사용자가 diary 박는 모든 지점) 에서 `delete entry._aiSummaryFailed; delete entry._aiSummaryFailReason;` 동시 실행

#### HIGH
- **A2-H1** dual-write `entry.photoStorageKeys` 와 `entry.photos`/`photo` 슬롯 길이 mismatch 가능
  - 위치: `17-mood-vitality-sleep.js:247-271`, `23-archive/06b-diary-media-edit.js:60-103, 257-282`
  - Fix: splice 직후 invariant 검증, 짧으면 null pad
- **A2-H2** `editTimelineEntry` 의 `editedAt` clear 지점 없음 (현재 무해, 향후 좀비)
  - Fix: `14-timeline-lens.js:280` delete 블록에 `delete entry.editedAt` 추가
- **A2-H3** `dailySource = 'diary'` 가 batch 결과 도착 시 'auto' 로 덮여 씀
  - 위치: `30-force-analyze.js:884, 1265`
  - Fix: 위 C1 가드 추가 시 동시 해결

#### MEDIUM
- **A2-M1** legacy `_pendingDiarySummary` 마이그 sweep이 1회성 flag — 백업/복원 후 잔존 가능
  - Fix: sweep을 flag 없이 매 load마다 unconditional로
- **A2-M2** `35-bg-fetch.js:76-78` _hasMissingDiary 가 오늘 entry 포함 (불필요 wake)
  - Fix: `e.date < todayDk` 가드 추가

#### Phase 1E Step 7 (별도)
- Phase 1E Step 7 (2026-07-01 자동 트리거) 의 사진 dual-write fallback path 정리가 H1 invariant fix 후 진행 가능

---

### 영역 3: Batch State Lifecycle (30-force-analyze.js, 09-reviews-future-self/, 04-annual-review-phase1/)

#### CRITICAL
- **A3-C1** `requests.length === 0` 분기에서 `lastChapterCleanupAt` stamp → 좀비 청소 전 사용자 24h cooldown 차단
  - 위치: `30-force-analyze.js:1101-1105`
  - Fix: 진성 unprocessed.length === 0 인 경우만 stamp (좀비 filter 거부 시는 stamp 안 함). 또는 zombie sweep을 step B 진입 전에 무조건 1회 실행
- **A3-C2** cleanup batch 12h not-ended timeout 분기 (_resumePendingBatch는 6h, cleanup은 12h) — 일관성 부족 (영향 작음, 24h hard cap 보완)

#### HIGH
- **A3-H1** annual `_processAnnualReviewResult` null 반환 시 sentinel 안 박힘 → 무한 재시도 가능
  - 위치: `30-force-analyze.js:1623-1636`
- **A3-H2** legacy `state.pendingBatch.submitted_at` ISO string vs number(ms) schema 불명확 → `Date.now() - submitted_at` NaN → 24h cap 발화 X → 영구 stuck
  - Fix: `_submittedMs = typeof pb.submitted_at === 'number' ? pb.submitted_at : new Date(pb.submitted_at).getTime()` + isFinite 가드
- **A3-H3** pendingReviewBatch `resultsFailCount` / `dataInvalidCount` success path에 reset 없음 (cosmetic)
- **A3-H4** PWA(Capacitor X) 환경에서 setTimeout polling이 백그라운드 suspend로 죽음 → batch 결과 지연
  - Fix: bg-fetch wake에서 보강 (이미 부분 적용, 데스크탑 path 추가 필요)

#### MEDIUM
- **A3-M1** zombie sweep이 `_cleanupRetryCount` 안 reset (영향 매우 작음)
- **A3-M2** review batch in-flight 중에도 cleanup step B 실행 — billing/concurrency race 가능
- **A3-M3** `09-reviews-future-self/04-open-review.js:3,62,120` 가 legacy `state.pendingBatch` 만 체크, 새 `state.pendingReviewBatch` 미체크 → 동일 review 가 batch + inline 동시 진행 가능
  - Fix: race guard에 `state.pendingReviewBatch?.review_keys?.some(rk => rk.cycle === type)` 추가

#### LOW
- **A3-L1** `35-bg-fetch.js:73` 가 새 `_pendingCleanup` + 옛 `_pendingExtract` 둘 다 통과 → 옛 마커만 박힌 archive가 submit filter에서 거부 → cycle 낭비
  - Fix: `_pendingCleanup` 만으로 narrowing (migration shim이 이미 변환)

---

### 영역 4: Review Markers (09-reviews-future-self/, 04-annual-review-phase1/, 27-monthly-rollup.js)

#### CRITICAL
- **A4-C1** weekly review `scenes` 스키마 mismatch — prompt는 string array, render는 object array
  - 위치: 스키마 `05-data-prompt-process.js:279-283` (string array 명시), render `06-render-screen.js:147,152-156` (`s.what / s.when / s.feeling` 접근), chain resume `30-force-analyze.js:1562`
  - 사용자 영향: 주간 리뷰 "📔 이번 주 장면" 카드 항상 누락
  - Fix: prompt schema를 `{when, what, feeling}` 객체 array로 변경 + chain resume도 같이

#### HIGH
- **A4-H1** chain resume의 annual `auto:true` 박은 후 saveState 누락 → reload 시 `auto:false` 영구화
  - 위치: `30-force-analyze.js:1631`
  - Fix: mutation 후 즉시 `saveState()` 또는 inline 객체 construction으로 통일
- **A4-H2** `_diagnoseExtract:1988` 이 weekly 강제 생성 후 `auto:false` 정정 누락
  - Fix: `_runReviewExtractInline`에 `opts.manual = true` 추가, manual이면 push 시 `auto: false` 박음
- **A4-H3** `_collectAnnualData` 의 chatArchive filter가 `c.createdAt` fallback 누락 (다른 collect 함수는 3중 fallback)
  - 위치: `04-annual-review-phase1/01-data-prompt-generate.js:22`
  - Fix: `inYear(c.generatedAt || c.createdAt || (c.date ? c.date + 'T12:00:00' : null))`
- **A4-H4** `deleteReview` 후 `_dismissedReviews` / `weeklyReviewsFailedKeys` 잔존 → 같은 weekKey 재생성 영원 X
  - 위치: `28-project-tracking/14-review-collection.js:31-52`
  - Fix: filter 후 `state.weeklyReviewsFailedKeys = state.weeklyReviewsFailedKeys.filter(k => k !== key)` + `_dismissedReviews` 도 strip

#### MEDIUM
- **A4-M1** `_processAnnualReviewResult` 의 filter+unshift unconditional replace → 중복 처리 시 completed `auto:true` 마커 잃음
  - Fix: caller가 existsAnnual 가드 후 push 책임지게 분리
- **A4-M2** `_runReviewExtractInline:973` `...json` spread → LLM이 schema 외 임의 field 반환 시 state 비대화
  - Fix: chain resume처럼 명시적 field picker 사용

#### LOW
- **A4-L1** `_quarterlyCutoffAt(0)` vs render-prompts hook의 `today.getMonth() - 1, 15` 한 달 어긋남 가능 (preview 데이터 미스매치)

---

### 영역 5: LLM Response Type Guards (다수 파일)

#### CRITICAL
- **A5-C1** `_processExtractChapterAnalysis` — `t.name.trim()` on non-string
  - 위치: `10-home/06-extract-insight.js:283-307` (traits), `:308-333` (values), `:334-359` (patterns)
  - 가드 코드: `if (!t || !t.name) return;` — truthy 체크만, object/array일 때 통과 후 throw
  - 사용자 영향: cleanup batch path + inline path 모두에서 throw → `_caseOk=false` → `_pendingCleanup` 보존 → 무한 재시도 (forceAnalyze와 동일 패턴)
  - Fix: `if (!t || typeof t.name !== 'string' || !t.name.trim()) return;` — similarText 패턴 그대로 적용
- **A5-C2** `30-force-analyze.js` traits/values/patterns `.forEach` non-name 가드 누락
  - 위치: `30-force-analyze.js:192-201, 204-213, 216-225`
  - 영향: similarText는 fix됐지만 dedupe 실패 시 dirty data 영구 저장 → render fail 가능
  - Fix: forEach 첫 줄에 `if (!t || typeof t.name !== 'string' || !t.name.trim()) return;`

#### HIGH
- **A5-H1** `21-insight-saving/04-strategy-from-chat.js:37-38` — `data.content[0].text.trim()` resp.ok 사전 체크 없음
  - Fix: `if (!resp.ok) throw new Error('API '+resp.status);` + optional chaining
- **A5-H2** `25-archive-daily/02-extract-topic-prompt.js:120-125` — `data.content[0].text.trim()` schema 미검증
  - Fix: optional chaining
- **A5-H3** `27-monthly-rollup.js:300-303` & `09-reviews-future-self/05-data-prompt-process.js:424-427` — `respData.content[0].text` 무가드
  - Fix: `const text = respData?.content?.[0]?.text || '';`
- **A5-H4** `21-insight-saving/08-mutation-chat-open.js:97-112` — `data.content[0].text.trim()` resp.ok 사전 체크 없음
  - Fix: optional chaining + resp.ok check
- **A5-H5** `09-reviews-future-self/05-data-prompt-process.js:358` — `m.content.slice(0,200)` on possible array (cache_control multi-block)
  - 사용자 영향: review batch prompt에 `"[object Object]"` 들어가 garbage
  - Fix: `(typeof m.content === 'string' ? m.content : Array.isArray(m.content) ? m.content.map(c=>c?.text||'').join(' ') : '').slice(0, 200)`

#### MEDIUM
- **A5-M1** `30-force-analyze.js:1648, 1673` — `(i.content || '').toLowerCase()` truthy non-string 시 throw
  - Fix: `typeof i.content === 'string' ? i.content.toLowerCase() : ''`
- **A5-M2** `21-insight-saving/24-auto-insight-discover.js:85, 112` — 동일 패턴
- **A5-M3** `19-chat/10-process-analysis.js:30-35` — `String()` 변환 사용으로 안전 (보고만)

#### LOW
- **A5-L1** `30-force-analyze.js:769` — `msg?.content?.[0]?.text || ''` guarded OK
- **A5-L2** `21-insight-saving/02-pearl.js:49-50` — guarded OK

---

### 영역 6: External Data Type Guards (03-auth/, 05-supabase.js, 32-billing/)

#### CRITICAL
- **A6-C1** `session.user.id` 무방어 접근 — refresh 응답에 user 누락 시 TypeError
  - 위치: `03-auth/01-session-otp.js:113-115`, `02-state.js:60-61`
  - Fix: `if (!ns || !ns.access_token || !ns.user) return false;` 가드 추가 (02-state.js:59 패턴 그대로)
- **A6-C2** anonymous signup 응답 `data.refresh_token` 무검증 → 1h 후 강제 logout
  - 위치: `03-auth/09-anonymous-guest.js:32-40`
  - Fix: invalid_response 분기에 `!data.refresh_token` 추가

#### HIGH
- **A6-H1** cron snap 복원 시 `cronRow.data.snap_at` 가정 + ts fallback 이 잘못된 ISO
  - 위치: `32-billing/15-manual-restore.js:5-34, 281`
  - Fix: shape 검증 + 잘못된 fallback 제거
- **A6-H2** `recoverFromBackup` 의 `rows[0].data` 가 옛 schema(V3/V5) 일 때 `migrateToV7` 미호출 → render crash 가능
  - 위치: `32-billing/16-migration-backup-recovery.js:9-22`
  - Fix: 복원 후 `if ((state.version||0) < 7) migrateToV7()` 추가
- **A6-H3** snap.data spread 후 caseFormulation/preferences sub-필드 보강 미적용 → 옛 schema backup 복원 시 forEach undefined crash
  - 위치: `32-billing/15-manual-restore.js:195, 352`
  - Fix: `loadFromCloud:367-396` 의 보강 블록을 helper로 추출 → restore path에서도 호출

#### MEDIUM
- **A6-M1** `_loadChapterMessages` / `_hydrateChatArchiveOnLoad` 의 `JSON.parse(plain)` 결과 무검증
  - 위치: `05-supabase.js:794-797, 1005-1008`
  - Fix: `parsed && typeof parsed === 'object' && parsed.role` 가드
- **A6-M2** `_e2eeRestoreFromPassphrase` decrypt 후 `JSON.parse` 실패 시 status text만 set, state 빈 채 reload 안 됨
  - 위치: `03-auth/07-e2ee-recovery-modal.js:147-151`
  - Fix: 별도 try/catch + "blob 손상" 알림 강화

---

## 권장 fix 순서

### Wave 1 — 즉시 (이번 주, 출혈 막기)
1. **Prompt 1**: LLM response type guards (A5-C1, A5-C2, A5-H1~H5, A5-M1~M2) — forceAnalyze 무한 루프 패턴 재발 방지
2. **Prompt 2**: Archive 마커 좀비 청소 + 삭제 path fix (기존 zombie 프롬프트 + A1-C2, A1-H1, A1-M2)
3. **Prompt 3**: Entry/diary 마커 fix (A2-C1, A2-C2, A2-H3)

### Wave 2 — 다음 주 (UI / 사용자 가시 버그)
4. **Prompt 4**: Review 시스템 schema mismatch + idempotency (A4-C1, A4-H1~H4)
5. **Prompt 5**: Auth + backup restore robustness (A6-C1, A6-C2, A6-H2, A6-H3)

### Wave 3 — 시간 날 때 (Hygiene / future-proofing)
6. **Prompt 6**: Defensive invariant layer at saveState — 미래 버그 즉시 감지

### 잡다 (개별 진행)
- A1-C1 `_extractFromIndex` clear (Wave 1에 합치거나 별개)
- A1-H2 cleanup done 마커 → ISO timestamp (큰 schema 변경, 별개 진행)
- A2-H1 dual-write photoStorageKeys invariant (Phase 1E Step 7과 연결, 7월 1일 trigger 전)
- A3-M3 04-open-review.js race guard (Wave 2에 합칠 수 있음)

---

## Claude Code 프롬프트 묶음

각 프롬프트는 독립적으로 새 Claude Code 세션에 복사-붙여넣기 가능. 빌드+verify 단계 포함.

### Prompt 1 — LLM response type guards

```
배경 — 소라고동 codebase에서 forceAnalyze가 매 init마다 무한 크래시 루프 도는 버그 (`TypeError: s.toLowerCase is not a function`) 진단 후, 동일 패턴(외부 데이터에 타입 가정 없이 메소드 호출)을 codebase 전체에서 audit함. 9개 위치에서 같은 패턴 발견. similarText는 이미 fix됨 — 동일 패턴 적용.

# 수정 task (모두 src/ 만 수정, 빌드 후 verify 필수)

## CRITICAL 2개

### [1-1] src/scripts/main/10-home/06-extract-insight.js 의 traits/values/patterns forEach typeof 가드
- 위치: line 283-307 (traits), 308-333 (values), 334-359 (patterns)
- 현재: `if (!t || !t.name) return;`
- 수정: `if (!t || typeof t.name !== 'string' || !t.name.trim()) return;`
- 같은 함수 안 `t.description`, `t.quiz_question`도 호출 전 typeof string 체크 후 `.trim()`

### [1-2] src/scripts/main/30-force-analyze.js traits/values/patterns forEach 가드
- 위치: line 192-201 (traits), 204-213 (values), 216-225 (patterns)
- 각 forEach 첫 줄에 `if (!t || typeof t.name !== 'string' || !t.name.trim()) return;` (또는 v.name, p.name)
- mergeStrings(line 232-239)는 이미 가드 있음 — 동일 패턴

## HIGH 5개

### [1-3] src/scripts/main/21-insight-saving/04-strategy-from-chat.js:37-38
- resp.ok 체크 + optional chaining
- 현재: `const data = await resp.json(); let raw = data.content[0].text.trim();`
- 수정:
  ```js
  if (!resp.ok) throw new Error('API '+resp.status);
  const data = await resp.json();
  let raw = (data?.content?.[0]?.text || '').trim();
  if (!raw) throw new Error('empty response');
  ```

### [1-4] src/scripts/main/25-archive-daily/02-extract-topic-prompt.js:120-125
- `let text = data.content[0].text.trim();` → `let text = (data?.content?.[0]?.text || '').trim();`

### [1-5] src/scripts/main/27-monthly-rollup.js:300-303
- `const text = respData.content[0].text;` → `const text = respData?.content?.[0]?.text || '';`
- 동일하게 src/scripts/main/09-reviews-future-self/05-data-prompt-process.js:424-427

### [1-6] src/scripts/main/21-insight-saving/08-mutation-chat-open.js:97-112
- resp.ok 사전 체크 + optional chaining (위 [1-3] 패턴 동일)

### [1-7] src/scripts/main/09-reviews-future-self/05-data-prompt-process.js:358
- 현재: `chatInRange.map(m => m.content.slice(0, 200)).join(...)`
- 수정:
  ```js
  chatInRange.map(m => {
    const c = m && m.content;
    const s = typeof c === 'string' ? c
      : Array.isArray(c) ? c.map(b => b?.text || '').join(' ')
      : '';
    return s.slice(0, 200);
  }).join(...)
  ```

## MEDIUM 2개

### [1-8] src/scripts/main/30-force-analyze.js:1648, 1673
- `(i.content || '').toLowerCase()` → `typeof i.content === 'string' ? i.content.toLowerCase() : ''`

### [1-9] src/scripts/main/21-insight-saving/24-auto-insight-discover.js:85, 112
- 동일 패턴 적용

# 빌드 + verify
```bash
npm run build
npm run verify
```

# 커밋
```bash
git add src/ index.html public/index.html
git commit -m "fix: LLM response type guards 전수 적용

forceAnalyze 무한 크래시 루프 (TypeError: s.toLowerCase) 와 동일 패턴이 9개 위치에 있어 일괄 fix. 외부 데이터(LLM 응답)에 타입 가정 없이 메소드 호출하던 곳 모두 typeof 가드 + optional chaining 추가. defensive skip + 빈 string fallback 으로 throw 차단."
```
```

### Prompt 2 — Archive 마커 좀비 청소 + 삭제 path fix

```
배경 — chapter cleanup batch가 매일 새벽 silent no-op 상태인 버그를 진단했어. ROOT CAUSE: archive 삭제 시 `_pendingCleanup` 마커가 strip 안 돼서 좀비 archive 발생. 추가로 audit에서 발견한 마커 위생 문제 4개 합쳐 일괄 처리.

# 수정 task (모두 src/ 만 수정)

## CRITICAL 1개

### [2-1] sweep 확장 — `_deleted` archive 의 잔존 마커 청소
- 파일: `src/scripts/main/30-force-analyze.js`
- 위치: line 1825-1844 (msg<3 stuck marker sweep 블록)
- 현재: `if (!a || a._deleted) return;` 로 좀비 영원히 skip
- 수정:
  ```js
  let _sweptDeadMarker = false;
  (state.chatArchive || []).forEach(a => {
    if (!a) return;
    // V4 fix (2026-05-26 audit): 좀비 (_deleted + 잔존 마커) 청소
    if (a._deleted) {
      if (a._pendingCleanup || a._pendingExtract || a._pendingCaseAnalysis || a._batchSubmittedAt) {
        delete a._pendingCleanup;
        delete a._pendingExtract;
        delete a._pendingCaseAnalysis;
        delete a._batchSubmittedAt;
        _sweptDeadMarker = true;
      }
      return;
    }
    // multi-device race: pendingChapterCleanupBatch null 인데 _batchSubmittedAt 잔존 시 strip
    if (!state.pendingChapterCleanupBatch?.batch_id && a._batchSubmittedAt) {
      delete a._batchSubmittedAt;
      _sweptDeadMarker = true;
    }
    // 기존 msg<3 sweep
    if (a._pendingCleanup || a._pendingExtract || a._pendingCaseAnalysis) {
      const _msgs = a.messages;
      const _invalid = !Array.isArray(_msgs) || _msgs.length < 3;
      if (_invalid) {
        delete a._pendingCleanup;
        delete a._pendingExtract;
        delete a._pendingCaseAnalysis;
        delete a._batchSubmittedAt;
        _sweptDeadMarker = true;
      }
    }
  });
  ```

### [2-2] archive 삭제 path — `_pendingCleanup` 동시 strip
- grep으로 모든 `_deleted = true` 박는 자리 찾기:
  ```bash
  grep -rn "_deleted = true\|_deletedAt = " src/scripts/main/
  ```
- 각 자리에서 4줄 추가:
  ```js
  delete a._pendingCleanup;
  delete a._pendingExtract;
  delete a._pendingCaseAnalysis;
  delete a._batchSubmittedAt;
  ```
- helper로 추출 권장:
  ```js
  function _markArchiveDeleted(a) {
    if (!a) return;
    a._deleted = true;
    a._deletedAt = new Date().toISOString();
    delete a._pendingCleanup;
    delete a._pendingExtract;
    delete a._pendingCaseAnalysis;
    delete a._batchSubmittedAt;
  }
  ```

## HIGH 1개

### [2-3] `_extractFromIndex` clear at cleanup completion
- 파일: `src/scripts/main/30-force-analyze.js`
- 위치: line 1289 (`if (arch._caseAnalysisDone && arch._topicExtractDone)` 분기)
- 추가: `delete arch._extractFromIndex;`
- 이유: 이어서-마무리 반복 archive의 옛 부분 영원히 분석에서 제외되는 문제 방지

## MEDIUM 1개

### [2-4] init boot-time sweep — `_chatMessagesSaveInFlight` strip
- 파일: src/scripts/main/07-init/01-init-fn.js 또는 적절한 init 함수
- 위치: 초기화 직후 어딘가
- 추가:
  ```js
  // V4 fix (2026-05-26 audit): tab close 시 잔존하는 in-flight 마커 strip
  (state.chatArchive || []).forEach(a => {
    if (a && a._chatMessagesSaveInFlight) delete a._chatMessagesSaveInFlight;
  });
  ```

# 빌드 + verify
```bash
npm run build
npm run verify
```

# 검증 시나리오
1. archive 하나 삭제 → reload → `state.chatArchive.filter(a => a._deleted && a._pendingCleanup).length === 0` 확인
2. sweep 작동 확인 — multi-device 시뮬: state에 `_batchSubmittedAt` 만 있고 `pendingChapterCleanupBatch` 가 null인 archive를 인위적으로 만든 후 reload → 마커 strip되는지

# 커밋
```bash
git add src/ index.html public/index.html
git commit -m "fix: archive 좀비 마커 청소 + 삭제 path 위생

- sweep 확장: _deleted archive 의 잔존 _pendingCleanup/_batchSubmittedAt 자동 청소
- archive 삭제 path: _deleted 박을 때 cleanup 마커 동시 strip (helper _markArchiveDeleted)
- _extractFromIndex clear: cleanup 완료 시 자동 reset (이어서-마무리 반복 archive 분석 누락 방지)
- init sweep: _chatMessagesSaveInFlight 잔존 마커 boot-time 청소

ROOT CAUSE: archive 삭제 시 _pendingCleanup 마커 안 지워서 좀비 발생 → cleanup batch filter 영구 거부 → 매일 새벽 no-op + lastChapterCleanupAt 만 stamp."
```
```

### Prompt 3 — Entry/diary 마커 fix

```
배경 — diary 작성 path와 batch 결과 도착 path의 race 조건 + sentinel 좀비 문제 일괄 fix.

# 수정 task (모두 src/ 만 수정)

## CRITICAL 2개

### [3-1] diary 작성 시 _aiSummaryFailed sentinel 동시 strip
- 파일: src/scripts/main/17-mood-vitality-sleep.js (line 178-180 근처 — entry.diary = ... 박는 자리)
- 파일: src/scripts/main/19-chat/05-send-chat.js (line 104-106 근처 — diary 작성 path)
- 각 자리에 추가:
  ```js
  // V4 fix (2026-05-26 audit): 사용자가 직접 diary 작성하면 sentinel strip
  delete entry._aiSummaryFailed;
  delete entry._aiSummaryFailReason;
  ```
- 이유: 그 옛 날에 diary 추가하면 batch가 재공급할 수 있게 권리 회복

### [3-2] batch 결과 도착 시 entry.diary 존재 확인 — 덮어쓰기 방지
- 파일: src/scripts/main/30-force-analyze.js
- 위치: line 880-888 (legacy diary summary path) 와 line 1262-1271 (new cleanup batch result path)
- 각 자리에서 `entry` lookup 후 summary 박기 전에 가드:
  ```js
  if (entry) {
    // V4 fix (2026-05-26 audit): 사용자가 batch 중간에 직접 diary 작성한 경우 덮어쓰기 X
    if (entry.diary) {
      console.log('[diary batch] user wrote diary while batch in flight — skip aiSummary', dateKey);
      return; // 또는 continue
    }
    if (summary && summary.length > 0) {
      entry.aiSummary = summary;
      entry.dailySource = 'auto';
    } else {
      entry._aiSummaryFailed = true;
      entry._aiSummaryFailReason = 'empty_batch_response';
    }
  }
  ```

## HIGH 1개

### [3-3] `editTimelineEntry` 의 `editedAt` clear
- 파일: src/scripts/main/14-timeline-lens.js (또는 25-archive-daily/14-timeline-lens.js)
- 위치: line 280 (`if (updated.trim() === '')` 분기, `delete entry.userEdit` 하는 자리)
- 추가: `delete entry.editedAt;`

## MEDIUM 1개

### [3-4] `35-bg-fetch.js:76-78` 오늘 entry 제외
- 파일: src/scripts/main/35-bg-fetch.js
- 위치: `_hasMissingDiary` 함수 내부
- 수정: 현재 `e.date` 만 비교 → `e.date < todayDk` 가드 추가 (todayDk 헬퍼 사용)

# 빌드 + verify
```bash
npm run build
npm run verify
```

# 검증 시나리오
1. 옛 entry 에 `_aiSummaryFailed: true` 인위 박기 → 그 날 entry에 diary 작성 → 마커 strip 확인
2. batch 시뮬: entry 의 diary 없음 → batch 결과 도착하면 aiSummary 박힘 → 정상

# 커밋
```bash
git add src/ index.html public/index.html
git commit -m "fix: entry/diary marker race + sentinel 좀비 청소

- _aiSummaryFailed sentinel: 사용자가 그 날 diary 직접 작성 시 자동 strip
- batch 결과 도착 시: entry.diary 존재하면 aiSummary 덮어쓰기 X (race guard)
- editTimelineEntry editedAt clear: userEdit 지울 때 editedAt 도 동시 strip
- bg-fetch: _hasMissingDiary 가 오늘 entry 포함하던 불필요 wake 제거"
```
```

### Prompt 4 — Review 시스템 schema + idempotency

```
배경 — review 시스템 (weekly/monthly/quarterly/annual) 의 schema mismatch + idempotency 버그 일괄 fix.

# 수정 task (모두 src/ 만 수정)

## CRITICAL 1개

### [4-1] weekly scenes schema 통일
- 파일 A: src/scripts/main/09-reviews-future-self/05-data-prompt-process.js (line 279-283)
- 파일 B: src/scripts/main/09-reviews-future-self/06-render-screen.js (line 147, 152-156)
- 결정: render 가 사용하는 `{when, what, feeling}` 객체 array로 통일
- 수정 A (prompt schema 변경):
  ```
  "scenes": [
    { "when": "월요일 저녁", "what": "엄마 통화 후 30분 멍", "feeling": "울컥" },
    ...
  ]
  ```
  대신 옛 string array 도 받게 render 측에 fallback 추가하는 게 안전:
- 수정 B (render fallback):
  ```js
  const scenesArr = (s.scenes || []).map(sc => {
    if (typeof sc === 'string') return { what: sc };
    return sc;
  }).filter(s => s && (s.what || s.when));
  ```
- chain resume 도 같이 통과: src/scripts/main/30-force-analyze.js:1562 (`scenes: json.scenes`) 유지 — render fallback 만으로 충분

## HIGH 4개

### [4-2] annual auto:true persist saveState
- 파일: src/scripts/main/30-force-analyze.js
- 위치: line 1623-1637 (annual chain resume push 후 auto=true mutation)
- 추가: mutation 직후 `saveState()` 호출 — 또는 더 깔끔하게 `_processAnnualReviewResult` 분리해서 review 객체만 반환, push는 caller가 담당

### [4-3] `_diagnoseExtract` weekly 강제 생성 시 auto:false 정정
- 파일: src/scripts/main/30-force-analyze.js
- 위치: line 1988 (`_runReviewExtractInline(['weekly'], ...)`)
- 옵션 A (작은 fix): `_forceWeeklyReview:1921-1927` 패턴 그대로 복붙
- 옵션 B (큰 fix): `_runReviewExtractInline` 에 `opts.manual = true` 추가, manual 일 때 push 시 `auto: false` 박음 — 1925 정정 블록 제거 가능

### [4-4] `_collectAnnualData` createdAt fallback 추가
- 파일: src/scripts/main/04-annual-review-phase1/01-data-prompt-generate.js (line 22)
- 현재: `inYear(c.generatedAt || (c.date ? c.date + 'T12:00:00' : null))`
- 수정: `inYear(c.generatedAt || c.createdAt || (c.date ? c.date + 'T12:00:00' : null))`

### [4-5] `deleteReview` 후 dismissedReviews + failedKeys 청소
- 파일: src/scripts/main/28-project-tracking/14-review-collection.js (line 31-52)
- 추가:
  ```js
  // V4 fix (2026-05-26 audit): 삭제 후 dismissedReviews + failedKeys 도 청소 — 재생성 가능
  const keyField = type === 'weekly' ? 'weekKey' : type === 'monthly' ? 'monthKey'
    : type === 'quarterly' ? 'quarterKey' : 'year';
  const deletedKey = reviewToDelete[keyField];
  const failedKeysField = `${type}ReviewsFailedKeys`;
  if (Array.isArray(state[failedKeysField])) {
    state[failedKeysField] = state[failedKeysField].filter(k => k !== deletedKey);
  }
  if (state.preferences?._dismissedReviews?.[type] === deletedKey) {
    delete state.preferences._dismissedReviews[type];
  }
  ```

## MEDIUM 1개

### [4-6] `09-reviews-future-self/04-open-review.js:3,62,120` race guard에 pendingReviewBatch 추가
- 각 자리의 `state.pendingBatch?.review_pending?.includes(type)` 같은 가드 옆에:
  ```js
  || state.pendingReviewBatch?.review_keys?.some(rk => rk.cycle === type)
  ```

# 빌드 + verify + 커밋
```bash
npm run build
npm run verify
git add src/ index.html public/index.html
git commit -m "fix: review schema mismatch + idempotency

- weekly scenes: string array vs object array fallback 추가
- annual chain resume: auto=true mutation 후 즉시 saveState
- _diagnoseExtract: weekly 강제 생성 시 auto=false 정정
- _collectAnnualData: createdAt fallback 추가 (다른 collect 함수와 통일)
- deleteReview: dismissedReviews + failedKeys 청소 → 재생성 가능
- open-review: race guard에 pendingReviewBatch 추가"
```
```

### Prompt 5 — Auth + backup restore robustness

```
배경 — auth refresh / signup 응답 shape 가정 + backup restore 시 schema migration 누락 일괄 fix.

# 수정 task (모두 src/ 만 수정)

## CRITICAL 2개

### [5-1] session refresh 응답 user 누락 가드
- 파일 A: src/scripts/main/03-auth/01-session-otp.js (line 113-115)
- 파일 B: src/scripts/main/02-state.js (line 60-61, 이미 가드 있음 — A에 동일 적용)
- 수정 A:
  ```js
  const newSession = await refreshResp.json();
  if (!newSession || !newSession.access_token || !newSession.user) {
    console.warn('[refresh] invalid response shape — skip');
    return false;
  }
  session = { ...session, access_token: newSession.access_token, ..., user: newSession.user };
  authUserId = newSession.user.id;
  ```

### [5-2] anonymous signup refresh_token 검증
- 파일: src/scripts/main/03-auth/09-anonymous-guest.js (line 32-40)
- 수정:
  ```js
  if (!data?.access_token || !data?.user?.id || !data?.refresh_token) {
    return { ok: false, reason: 'invalid_response' };
  }
  ```

## HIGH 2개

### [5-3] `recoverFromBackup` migrateToV7 호출
- 파일: src/scripts/main/32-billing/16-migration-backup-recovery.js (line 9-22)
- 추가 — `state = { ...DEFAULT_STATE, ...backupData };` 직후:
  ```js
  // V4 fix (2026-05-26 audit): 옛 schema(V3/V5) backup 복원 시 V7 strict 필드 보강
  if ((state.version || 0) < 7) {
    if (typeof migrateToV7 === 'function') migrateToV7();
  }
  ```

### [5-4] restore path schema 보강 helper 추출
- 파일: src/scripts/main/02-state.js 또는 적절한 helper 파일
- 새 helper:
  ```js
  function _ensureV7Schema() {
    state.caseFormulation = state.caseFormulation || { unverified: {}, problems: [], mechanisms: [], strengths: [] };
    state.caseFormulation.unverified = state.caseFormulation.unverified || {};
    // 다른 V7 strict 필드 보강 (loadFromCloud:367-396 그대로 복붙)
    // ... (existing loadFromCloud 보강 블록 참조)
  }
  ```
- 호출처 추가:
  - src/scripts/main/32-billing/15-manual-restore.js (line 195, 352) — `state = { ...DEFAULT_STATE, ...snap.data };` 직후 `_ensureV7Schema()` 호출
  - src/scripts/main/05-supabase.js 의 loadFromCloud — 기존 보강 블록을 helper 호출로 교체

# 빌드 + verify + 커밋
```bash
npm run build
npm run verify
git add src/ index.html public/index.html
git commit -m "fix: auth/restore robustness

- refresh 응답 shape 검증 (user 누락 시 graceful skip)
- anonymous signup refresh_token 누락 시 invalid_response
- recoverFromBackup: 옛 schema backup 복원 시 migrateToV7 호출
- restore path: _ensureV7Schema helper 추출, loadFromCloud + manual-restore 둘 다 사용"
```
```

### Prompt 6 — Defensive invariant layer

```
배경 — 향후 새 버그가 비슷한 패턴(마커 위생 위반)으로 들어왔을 때 즉시 감지하기 위한 런타임 invariant 체크 추가.

# 수정 task (src/scripts/main/02-state.js 또는 saveState 정의 파일)

### [6-1] saveState 안에 invariant 체크 함수
- 추가 위치: saveState 함수 안, save 직전
- 코드:
  ```js
  // V4 (2026-05-26 audit): defensive invariant layer — 마커 위생 위반 즉시 console.warn
  function _assertStateInvariants() {
    // archive: _deleted + _pendingCleanup 동시 보유 → 좀비
    const zombies = (state.chatArchive || []).filter(a =>
      a && a._deleted && (a._pendingCleanup || a._pendingExtract || a._pendingCaseAnalysis || a._batchSubmittedAt)
    );
    if (zombies.length > 0) {
      console.warn('[INVARIANT] zombie archives:', zombies.length, zombies.map(a => a.id?.slice(-8)));
    }
    // archive: _batchSubmittedAt 박혔는데 state.pendingChapterCleanupBatch 없음 → multi-device race
    if (!state.pendingChapterCleanupBatch?.batch_id) {
      const orphanSubmits = (state.chatArchive || []).filter(a => a && a._batchSubmittedAt && !a._deleted);
      if (orphanSubmits.length > 0) {
        console.warn('[INVARIANT] orphan _batchSubmittedAt without pendingBatch:', orphanSubmits.length);
      }
    }
    // entry: _aiSummaryFailed 박혔는데 사용자가 diary 직접 작성 → sentinel 좀비
    const failedWithDiary = (state.entries || []).filter(e => e && e._aiSummaryFailed && e.diary);
    if (failedWithDiary.length > 0) {
      console.warn('[INVARIANT] _aiSummaryFailed + entry.diary coexist:', failedWithDiary.length, failedWithDiary.map(e => e.date));
    }
    // traits/values/patterns: name 이 string 아닌 항목 (dirty data 검출)
    ['traits', 'values', 'patterns'].forEach(k => {
      const dirty = (state[k] || []).filter(x => x && typeof x.name !== 'string');
      if (dirty.length > 0) {
        console.warn(`[INVARIANT] ${k} 에 비-string name:`, dirty.length, dirty.slice(0, 3));
      }
    });
    // review: _dismissedReviews / failedKeys 에 있는데 review 객체는 array 에 없음 (deleted 후 청소 누락)
    ['weekly', 'monthly', 'quarterly', 'annual'].forEach(type => {
      const failedKeys = state[`${type}ReviewsFailedKeys`] || [];
      const reviews = state[`${type}Reviews`] || [];
      const keyField = type === 'weekly' ? 'weekKey' : type === 'monthly' ? 'monthKey'
        : type === 'quarterly' ? 'quarterKey' : 'year';
      const orphanFailedKeys = failedKeys.filter(k => !reviews.some(r => r[keyField] === k));
      // failed key 가 review 에 없는 건 정상 (failed 라서) — 실제 검사할 것은 review 가 있는데 failed key 도 있는 경우
      const conflictKeys = failedKeys.filter(k => reviews.some(r => r[keyField] === k));
      if (conflictKeys.length > 0) {
        console.warn(`[INVARIANT] ${type} review 있는데 failed key 잔존:`, conflictKeys);
      }
    });
  }
  
  // saveState 안에서 호출:
  // try { _assertStateInvariants(); } catch (e) { console.warn('[invariant check]', e); }
  ```
- saveState 내부 적절한 자리에 try/catch 로 호출 (assert 자체가 throw 해서는 안 됨)

# 빌드 + verify + 커밋
```bash
npm run build
npm run verify
git add src/ index.html public/index.html
git commit -m "feat: defensive invariant layer at saveState

마커 위생 위반 + dirty data 를 saveState 직전에 console.warn 으로 즉시 노출.
체크 항목:
- archive 좀비 (_deleted + _pendingCleanup)
- archive 의 orphan _batchSubmittedAt (multi-device race)
- entry 의 _aiSummaryFailed + diary 공존 (sentinel 좀비)
- trait/value/pattern name 비-string (dirty data)
- review failed key 와 review 객체 동시 존재 (deleteReview 청소 누락)

미래 새 버그가 비슷한 패턴으로 들어와도 console 에 즉시 노출됨."
```
```

---

## 진행 순서

1. **Prompt 1** 먼저 (출혈 막기 — forceAnalyze 무한 루프 종결)
2. **Prompt 2** (좀비 archive 청소 + 삭제 path)
3. **Prompt 3** (entry/diary race + sentinel)
4. 1-3 완료 후 동작 확인 (1-2일)
5. **Prompt 4** (review schema)
6. **Prompt 5** (auth/restore)
7. **Prompt 6** (invariant layer — 끝에)

각 prompt 후 reload + console 확인. invariant layer (Prompt 6) 가 들어가면 그 후 1-2주 사용하면서 새 warning 보이는지 모니터.
