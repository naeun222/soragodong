<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- V4 (사용자 명시 2026-05-17 ultrathink) — App Links / deep link.
                 autoVerify=true 와 public/.well-known/assetlinks.json 의 SHA256 fingerprint
                 가 일치하면 https://soragodong.com 링크를 브라우저 대신 앱이 받음.
                 Bubblewrap TWA 의 LauncherActivity 가 갖던 같은 verification path 공유. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="soragodong.com" />
            </intent-filter>

            <!-- V4 fix (사용자 보고 2026-05-18 ultrathink) — Capacitor OAuth callback deep link.
                 @capacitor/browser 로 Chrome Custom Tabs 띄워 카카오 OAuth (카카오톡 SSO 가능) 처리.
                 Supabase redirect_to = com.soragodong.app://oauth-callback#access_token=...&refresh_token=...
                 → 이 intent-filter 가 캡처 → 앱이 받아 03-auth/10-capacitor-oauth-deeplink.js 의 listener 가 처리. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.soragodong.app" android:host="oauth-callback" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- V4 fix (사용자 보고 2026-05-18 ultrathink) — 갤럭시 위치 동의 모달 안 뜨던 버그.
         root cause: Capacitor WebView 의 navigator.geolocation 은 OS 권한 없으면 silent fail → _fetchCurrentWeather 가 _weatherPermission='denied' 영구 set → 다음 체크인부터 동의 prompt 자체 skip.
         fix: @capacitor/geolocation 플러그인 + ACCESS_COARSE_LOCATION (FINE 불필요 — 날씨 lat/lon 0.01° 반올림).
         16-modes.js 의 _fetchCurrentWeather 가 Capacitor.Plugins.Geolocation 우선 사용. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
</manifest>
