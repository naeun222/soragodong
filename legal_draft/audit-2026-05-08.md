# 종합 감사 보고 — 2026-05-08

> 변호사·심사관 관점 전수조사. 5 도메인 audit (billing / database / backend / auth / state) + 메인 직접 검토.
> commit `70794c0` 시점 기준. 출시 전 최종 점검.

---

## 한 줄 요약

소라고동은 **성인 컨텐츠 X 명백**. 토스/카카오 오분류는 약관 만 19세 정책이 트리거. → 만 14세 표준화 완료. **출시 전 처리 필요한 FAIL 8건 + WARN 20건**.

---

## 직접 fix 완료 (이번 commit)

1. ✅ **약관 가격 mismatch** — terms §4 / refund.md / public/terms.html 모두 9,900 / 25,000 / 4,900 / 추가팩 1,500·2,500 으로 정합
2. ✅ **자동 갱신 사전고지 7일** — terms §4.3 + public/terms.html. 옛 *3일* (콘텐츠산업진흥법 §25 위반) → 7일
3. ✅ **통신판매업 신고번호** — terms.md / privacy.md / refund.md `[TBD]` → `2026-서울동작-0613`
4. ✅ **얼리버드 4,900원 평생 보장** 약관에 명시 — 분쟁 시 강제력 확보
5. ✅ **Sentry / Resend 위탁자 추가** — privacy.md §4 / public/privacy.html
6. ✅ **카카오 OAuth scope = 이메일만** — privacy.md / public/privacy.html (출생연도·닉네임·프로필사진 신청 X 명시)
7. ✅ **CI workflow 정정** — `.github/workflows/ci.yml` (옛 vite/vitest 가정 → concat-build / verify only)

---

## 🔴 FAIL — 출시 전 사용자 결정 필요

### 1. 자살예방법 §15-6 서버측 위기 대응 X
- **위치**: `functions/api/chat.ts` (위기 키워드 감지가 클라이언트 전용)
- **위험**: PG 심사 차단 가능성. 정신건강 서비스 운영자 위기개입 의무.
- **권고**: 서버측 응답 post-process — Anthropic 응답에 자살/자해 키워드 detect 시 표준 disclaimer 강제 삽입 또는 별도 응답 헤더 추가.
- **결정 필요**: 도입 / 보류 (현재 클라이언트 가드만으로 충분하다고 판단 시).

### 2. 결제 직전 부가세 / 임상 치료 대체 X 고지 누락
- **위치**: `src/scripts/main/32-billing/08-subscribe-modal.js:130-132`
- **약관 §4.4** 6항목 중 (2) "부가세 포함 여부" / (6) "임상 치료 대체 X 고지" 모달에 없음.
- **위험**: 표시광고법 §3 위반 + PG 심사 거부 사유.
- **권고**: 결제 모달 하단 note 에 명시 — "부가세 10% 포함 / 본 서비스는 임상 치료·진단·전문가 상담을 대체하지 않습니다 (자세한 안내 →)"

### 3. 수정 영수증 자동 발급 미구현
- **위치**: `functions/api/billing/refund.ts` (PortOne `receiptUrl` 활용 X)
- **약관 §2.4 약속 X** — 코드 없음.
- **권고**: refund.ts 에서 PortOne receiptUrl 받아 payments 테이블에 저장 + 사용자 이메일 / 인박스 전달.

### 4. exportData 평문 파일 노출
- **위치**: `src/scripts/main/32-billing/12-opus-limit-modal.js:22`
- **위험**: PIPA §29 안전조치 의무. E2EE 활성 사용자도 export 파일은 평문 — 기기 분실 시 위험.
- **권고**: 두 옵션
  - (a) 비밀번호로 암호화된 형태로 export
  - (b) export 직전 경고 모달 ("이 파일은 암호화되지 않습니다. 안전한 곳에 보관해주세요")

### 5. 30일 미접속 / 6개월 자동 삭제 코드 미존재
- **위치**: 서버측 cleanup job 부재 (`functions/api/`, supabase migrations 모두 X)
- **terms §8.2 약속 vs 실제 X** = 약관 이행 미비.
- **권고**: Supabase scheduled function 또는 Cloudflare Cron Trigger 추가. `state.lastActiveAt` 필드 도입 + 6개월 cap.

### 6. 자동 갱신 사전고지 7일 알림 코드 미구현
- **위치**: `functions/api/billing/cron-charge-recurring.ts` (사전 알림 로직 X)
- **콘텐츠산업진흥법 §25 위반 위험** (약관은 이번 fix 로 7일 명시했으나 코드는 0일).
- **권고**: cron 흐름에 `next_billing_at <= now + 7d AND notified_7d_at IS NULL` 분기 추가 — 이메일/알림톡 발송 후 `notified_7d_at` 기록.

### 7. cron-charge-recurring paymentId 비멱등 (중복 결제 위험)
- **위치**: `functions/api/billing/cron-charge-recurring.ts:90`
- **위험**: cron 재시도 시 `Date.now() + Math.random()` 으로 paymentId 다른 거 생성 → 동일 사용자 중복 결제 가능.
- **권고**: `paymentId = recurring-${user_id}-${YYYY-MM-DD}` 결정적 ID 사용.

### 8. subscribe-modal "모두 자동 갱신" 표시 vs 실제 단건 결제 mismatch
- **위치**: `src/scripts/main/32-billing/08-subscribe-modal.js:132`
- **표시 광고법 §3** 혼동 유발. Light/Premium = 단건 / 얼리버드 = 자동.
- **권고**: 모달 하단 note 정정 — "Light/Premium = 단건 결제 (매월 수동) / 얼리버드 = 자동 갱신 (해지 1-click)"

---

## 🟡 WARN — 출시 후 권고

### 결제 / 환불
9. **legacy charge V1 환불 PortOne V2 호환** — 옛 imp_uid 형식 결제 환불 시 502 가능. 수동 처리 절차 문서화 필요.
10. **추가팩 30일 청약철회 제한** 코드 미구현 — 정책보다 관대 (소비자 유리).
11. **portone-webhook 환불 기록 불완전** — 외부 환불 시 `refund_amount_krw / refunded_at` 미기록 (전자상거래법 §6 5년 보존 데이터 결손).
12. **카드 등록 자동 결제 동의 체크박스 X** — 얼리버드 카드 등록 시 명시적 동의 UI 부재 (전자상거래법 §17 청약 확인 기회).

### 보안 / 인증
13. **카카오 신규 가입자 consentLog 미기록** — 이메일 OTP 경로와 일관성 X. PIPA 분쟁 시 타입별 동의 증거 부재.
14. **마스터 키 raw B64 localStorage** — XSS 시 마스터 키 직접 탈취 가능. CSP 강화 필요.
15. **E2EE recovery cloud 저장 실패 reload** — 루프 가능성. `_07-e2ee-recovery-modal.js:93-101`.

### 백엔드
16. **error-report.ts 인증 X** — Resend 무료 100/일 스팸 소진 가능. rate limit 추가 필요.
17. **recordGuestCost KV race** — 동시 요청 시 글로벌 예산 오버슈팅 가능 (소규모).
18. **account/delete 오류 본문 클라이언트 노출** — PIPA §29 내부 정보 leak 위험.
19. **admin/feedback-list status 화이트리스트 X** — Supabase query error 가능.
20. **의료법 §27 system prompt 가드 클라이언트 전용** — body 직접 호출 시 우회 가능. 서버측 minimum 가드 필요.

### 데이터베이스
21. **소비자 동의 로그 분리 테이블 X** — `consentLog` 가 user data row 안에 묻힘. 분쟁 시 추출 어려움. 권고: `soragodong_consent_log` 별도 테이블.
22. **payments user_id UUID 잔존** — 탈퇴 후 `user_email` 만 익명화 / `user_id` UUID 그대로. PIPA 식별 가능 정보. 권고: `user_id = NULL`.
23. **export 권리 cloud 데이터 미포함** — `exportData()` 가 localStorage 만 덤프. cloud 의 다른 device sync / `soragodong_usage` 미포함. 권고: `/api/account/export` endpoint 추가.

### State / E2EE
24. **E2EE sensitiveKeys whitelist 방식** — 신규 필드 누락 시 평문 cloud 저장 위험. blocklist 로 전환 권고.
25. **optimistic paint 중 수정 race** — cloud fetch RTT 1-2초 동안 사용자 입력 덮어쓰기 가능.
26. **auto-backup 평문 cloud 저장** — privacy 약속 ("회사도 평문 못 봄") 과 괴리. 약관에 예외 명시 필요 또는 backup 도 KEK 암호화.
27. **logout 백엔드 실패 시 "그래도 진행" 경로** — PIPA §36 즉시 이행 의무 취약.
28. **reload 직전 saveToCloudNow await 누락** — `07-init/06-onb-event-listeners.js:807` cloud sync 미완료 가능.

### 광고 / 마케팅
29. **외부 analytics 도구 0** ✓ PASS — Google Analytics / Pixel / Mixpanel 모두 X.

---

## ✅ PASS (검증된 항목)

- RLS 전 테이블 정상 (Cross-user 차단)
- service_role / Anthropic key 클라이언트 노출 X
- E2EE PBKDF2 1M iterations / AES-256-GCM 정확
- 다른 사용자 detect → localStorage wipe
- testerMode 격리
- atomic RPC race-safe (FOR UPDATE)
- portone-webhook svix 서명 검증
- portone-verify-pay 멱등 (Prefer: resolution=ignore-duplicates)
- admin endpoint 권한 (ADMIN_USER_ID)
- pruneOldChatArchive pinned 면제
- 시드 sweep 안전 (testerMode 격리)
- memo type AI 제외 (시스템 프롬프트 / force-analyze / monthly / annual / quarterly 모두)
- 자살 키워드 / 1393 안내 (carousel)
- 시스템 프롬프트 §15 임상 효능 시사 X 가드 (의료법 §27)

---

## 외부 시스템 액션 (사용자 직접)

### 토스페이
- **회신 송부**: `legal_draft/toss-입점-회신-초안.md` — 성인 컨텐츠 오분류 정정 + 약관 v1.3 갱신본 링크
- 만 14세 표준화 완료 → 자동분류기 트리거 제거
- 상태: 1차 반려 → 정정 회신 후 재심사 대기

### 카카오 디벨로퍼
- **출생연도 동의 항목 신청 철회** — 콘솔에서 직접 또는 데브톡 (`legal_draft/kakao-devtalk-문의-초안.md`)
- 처리방침 v1.4 갱신본 (위탁자 카카오 명시 + scope 이메일만) 검수 확인

### KG이니시스
- **계약담당자 컨택** ☎ 1670-1330 → "포트원 통해 가맹점 신청한 [나은랩 / 261-21-02592] 계약 담당자 연결"
- **보증보험 가입** (정기결제 ID 발급 필수)
- 또는 포트원 콘솔 → 결제연동 → 채널관리 → KG이니시스 채널 = 계약 상태 + 담당자 정보

### 포트원
- ☎ 1670-7077 / contact@portone.io
- 콘솔: https://admin.portone.io
- 채널 활성화 후 결제 테스트 (subscribe / overage / refund 흐름)

### CI
- ✅ 이번 fix 로 unit test step 제거. push 후 CI 통과 예정.

---

## 우선순위 제안

**🔴 출시 전 필수 (사용자 결정 받아 일괄 처리)**:
1. FAIL #2 (결제 모달 부가세·임상치료 대체 X 고지)
2. FAIL #6 (cron 7일 사전 알림 — 콘텐츠산업진흥법 위반 risk)
3. FAIL #7 (cron paymentId 멱등 — 중복 결제 위험)
4. FAIL #8 (subscribe-modal "모두 자동 갱신" 표시 정정)
5. FAIL #1 (자살예방법 §15-6 서버측 — *옵션 — 입점심사 vs 안전 의무*)
6. FAIL #4 (exportData 평문 경고 — UX 1줄)

**🟡 베타 단계 권고 (출시 후 1-2주)**:
- WARN #11 #12 #16 #18 #19 #20 (백엔드 권한·로그·우회)
- WARN #13 #14 #15 (auth / E2EE 강화)
- WARN #21 #22 #23 (DB 분리 / 익명화 / export 완성)

**🟢 개선 (점진)**:
- WARN #9 #10 #17 #24-28
- 수정 영수증 발급 (FAIL #3) = 베타 사용자 < 100명 단계에서는 수동 처리 가능

---

## 결론

**현재 main HEAD = 70794c0 + 이번 commit (요금 정합 + 통신판매업 + 사전고지 7일 + 위탁자 추가 + CI fix)**

**남은 작업 = 사용자 결정 후 별도 commit**:
- 코드 측 FAIL 5건 (결제 모달 / cron 알림·멱등 / 자살예방 서버측 / exportData 경고)
- 사용자 노출 카피 정정 ("모두 자동 갱신" 등)
- 외부 송부 (토스 / 카카오 / KG이니시스)
